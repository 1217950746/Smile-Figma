{
    "version": "2.0.0",
    "type": "shell",
    "tasks": [
        {
            "label": "run",
            "command": "yarn run watch"
        },
        {
            "label": "build",
            "command": "yarn run build"
        }
    ]
}
